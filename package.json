{
  "name": "meet-mosaic",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "sirv . --port 3000 --dev",
    "build": "mkdir -p dist && cp -r index.html app.js styles.css Images dist/",
    "lint": "echo 'No lint step required'"
  },
  "devDependencies": {
    "sirv-cli": "^3.0.1"
  }
}
